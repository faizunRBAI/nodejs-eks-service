# GENERATED by UDAP Build Project from .udap/pipeline.yaml — do not
# edit by hand; change the spec and re-render.
name: destroy
'on':
  workflow_dispatch: {}
jobs:
  destroy:
    runs-on: ubuntu-latest
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
    steps:
    - uses: actions/checkout@v4
    - uses: azure/setup-kubectl@v4
      with:
        version: v1.33.0
    - name: Remove Kubernetes load balancers
      run: "NAMESPACE=\"${{ secrets.PROJECT_NAME }}\"\nif aws eks update-kubeconfig\
        \ --name \"${NAMESPACE}-eks\" --region \"$AWS_REGION\" 2>/dev/null; then\n\
        \  kubectl delete svc --all -n \"$NAMESPACE\" --ignore-not-found --timeout=5m\
        \ || true\n  kubectl delete namespace \"$NAMESPACE\" --ignore-not-found --timeout=5m\
        \ || true\n  echo \"Waiting for AWS to finish deleting the load balancer.\"\
        \n  sleep 120\nelse\n  echo \"Cluster already gone \u2014 skipping Kubernetes\
        \ cleanup.\"\nfi\n"
    - uses: hashicorp/setup-terraform@v3
      with:
        terraform_wrapper: false
    - name: Terraform destroy
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=${AWS_REGION}\"\n\
        terraform destroy -input=false -auto-approve\n"
      working-directory: infra
